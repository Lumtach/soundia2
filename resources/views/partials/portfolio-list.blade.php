@php($active = $projects[0] ?? null)
<div class="portfolio-showcase"><div class="portfolio-showcase__list" aria-label="Projects"><p class="portfolio-showcase__label">{{ $locale === 'ru' ? 'Наши проекты' : ($locale === 'lv' ? 'Mūsu projekti' : 'Our projects') }}</p>
@foreach($projects as $project)<article class="portfolio-showcase__item {{ $loop->first ? 'is-active' : '' }}" data-portfolio-project="{{ $project['id'] }}" data-image="{{ $project['image'] }}" data-title="{{ $project['title'][$locale] }}" tabindex="0"><span class="portfolio-showcase__number"></span><div class="portfolio-showcase__copy"><strong>{{ $project['title'][$locale] }}</strong><span>{{ trim(explode('·', $project['category'][$locale])[0]) }}</span><p>{{ str_replace(['маршрута в Istanbul:', 'maršrutam Istanbulā:', 'in Istanbul where'], ['маршрута:', 'maršrutam:', 'where'], $project['summary'][$locale]) }}</p>@if(!empty($project['audioUrl']))@include('partials.audio-player', ['src'=>$project['audioUrl'], 'duration'=>$project['duration'] ?? ''])@endif</div></article>@endforeach
<div class="portfolio-showcase__cta"><button class="button button--lg button--arrow-end hero__cta" type="button" data-modal-open="order-modal">{{ $locale === 'ru' ? 'Заказать услугу' : ($locale === 'lv' ? 'Pasūtīt pakalpojumu' : 'Order a service') }}</button></div></div>
@if($active)<aside class="portfolio-showcase__preview" aria-live="polite"><img data-portfolio-preview src="{{ $active['image'] }}" alt="{{ $active['title'][$locale] }}"><div class="portfolio-showcase__overlay"></div></aside>@endif</div>
